defaults: &defaults
  sftp:
    chroot: /var/www

  servers:
    types:
      nginx_server: Nginx 服务器
      dns_server: Dns 服务器

  dns:
    types: [A, CNAME, MX]

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults